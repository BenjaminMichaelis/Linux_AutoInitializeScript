# Run before every package
    sudo apt-get update
    sudo apt-get dist-upgrade --yes

bash vsliveshare_prereqs
bash setupcpp
bash setupgit

# Forward display
#   need to run and install xLaunch
#   export DISPLAY="`grep nameserver /etc/resolv.conf | sed 's/nameserver //'`:0"
